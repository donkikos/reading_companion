services:
  app:
    image: epub_ai_reader_ralph_app
    build: .
    volumes:
      - ./:/app
      - ./.data:/app/.data
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
